//Consider the following program:

#include <stdio.h>

int main(void) 
{
    printf("Parkinson's Law:\nWork expands so as to ");
    printf("fill the time\n");
    printf("available for its completation\n");


    return 0;
}

/* 
a. Indentify the directives and statements in this program
    -Directives:
        -#include <stdio.h>
    -Statements:
        -printf
        -return

b. What ouputs does the program produce
    -Parkinson's Law:
    Work expands so as to fill the time
    available for its completation
*/